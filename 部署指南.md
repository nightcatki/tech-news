# 科技晨报 · GitHub Pages 部署指南

> 当前目录是一个可直接部署的静态 PWA。适合手机浏览器打开后添加到主屏幕使用。

## 第 1 步：创建 GitHub 仓库

1. 打开 [github.com/new](https://github.com/new)
2. Repository name 建议填：`tech-news`
3. 选择 Public 或 Private 均可
4. 不要勾选 “Add a README”
5. 点击 Create repository

## 第 2 步：推送本地代码

如果你把远程仓库地址发给我，我可以继续帮你执行。手动命令如下：

```bash
git remote add origin https://github.com/<你的用户名>/tech-news.git
git push -u origin main
```

## 第 3 步：开启 GitHub Pages

本项目已包含 `.github/workflows/pages.yml`，推送到 `main` 后会自动发布。

1. 仓库页进入 Settings → Pages
2. Source 选择 GitHub Actions
3. 回到 Actions，等待 “Deploy static site” 运行成功
4. Pages 页面会显示访问地址：
   `https://<你的用户名>.github.io/tech-news/`

## 第 4 步：手机使用

- iPhone：Safari 打开网址 → 分享按钮 → 添加到主屏幕
- Android：Chrome 打开网址 → 右上角菜单 → 添加到主屏幕

添加后会以独立 App 窗口打开。

## 验收清单

- [ ] Actions 中 “Deploy static site” 全绿
- [ ] 打开 Pages 网址能看到“科技晨报”
- [ ] 中文标题、按钮、摘要显示正常
- [ ] 手机可以添加到主屏幕

## 后续接入每日新闻

当前已补齐 `data/index.json` 和 `data/latest.json`，用于保证网站可打开。后续只要把每日生成的数据写入 `data/`，前端会自动读取并展示。

## 配置智谱 GLM 翻译

仓库端自动翻译：

1. 打开仓库 Settings → Secrets and variables → Actions
2. 点击 New repository secret
3. Name 填：`ZHIPU_API_KEY`
4. Secret 粘贴智谱开放平台 API Key
5. 保存后进入 Actions → Generate daily news → Run workflow 手动跑一次

手机端临时翻译：

打开网页底部“我的”，填入智谱 API Key。Key 只会保存在当前浏览器的本地存储中。
